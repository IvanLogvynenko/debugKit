#include "../../include/basic.hpp"

int main() {
    LOG("You can output logs with LOG() and FULL_LOGGING header");
    ILOG("You can output logs with ILOG() and DEBUG header");
    EL("EL() stands for error logging and requires ERROR and DEBUG header");
}